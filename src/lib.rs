pub mod prize;
pub mod tickets;
pub mod user;
mod space_efficient_shuffler;

#[cfg(test)]
mod test_utils;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
